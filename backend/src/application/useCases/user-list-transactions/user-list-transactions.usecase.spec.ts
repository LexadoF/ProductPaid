import { UserListTransactionsUsecase } from './user-list-transactions.usecase';

describe('UserListTransactionsUsecase', () => {
  it('should be defined', () => {
    expect(new UserListTransactionsUsecase()).toBeDefined();
  });
});
