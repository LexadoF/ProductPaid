import { CheckTransactionStatusUsecase } from './check-transaction-status.usecase';

describe('CheckTransactionStatusUsecase', () => {
  it('should be defined', () => {
    expect(new CheckTransactionStatusUsecase()).toBeDefined();
  });
});
