import { TransactionCreateUsecase } from './transaction-create.usecase';

describe('TransactionCreateUsecase', () => {
  it('should be defined', () => {
    expect(new TransactionCreateUsecase()).toBeDefined();
  });
});
